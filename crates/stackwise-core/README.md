# stackwise-core

`stackwise-core` is the artifact analysis engine behind Stackwise.

It parses emitted binaries and produces versioned stack-analysis reports for the `stackwise` CLI and UI. The crate is primarily intended for Stackwise internals and tooling that wants to consume the same report model directly.
